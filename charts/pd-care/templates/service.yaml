apiVersion: v1
kind: Service
metadata:
  name: {{ include "pd-care.fullname" . }}
  labels:
    {{- include "pd-care.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "pd-care.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
