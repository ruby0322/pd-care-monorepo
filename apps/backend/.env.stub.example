# Deprecated as a standalone file: use apps/backend/.env.local.example instead
# (full host-local template including LINE_VERIFY_MODE=stub + 127.0.0.1 DB/S3).
# See docs/ops/local-dev-without-line.md.
LINE_VERIFY_MODE=stub
