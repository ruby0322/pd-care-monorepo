# Local verification without LINE: minimal override to merge into apps/backend/.env for stub mode
# only (keep every other value from .env.example / .env). Pair with
# apps/frontend/.env.local.example. See docs/ops/local-dev-without-line.md.
LINE_VERIFY_MODE=stub
